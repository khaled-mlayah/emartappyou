import 'package:emartappyou/consts/colors.dart';
import 'package:flutter/material.dart';

class SearchScrenn extends StatelessWidget {
  final String? title;
  const SearchScrenn({Key? key,this.title}):super(key: key);

  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: whiteColor,
      appBar: AppBar(),
      body: Container(),
    );
  }
}